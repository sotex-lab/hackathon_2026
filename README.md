# Overshoot Pray – Nigeria Map

## Setup

```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1   # Windows PowerShell
pip install -r requirements.txt
cd src
python app.py
```

## API

- `GET /` – UI sa mapom
- `GET /api/substations` – lista trafostanica (za mapu)